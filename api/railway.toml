[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["**"]

[deploy]
# Use TCP port check only (HTTP /health can race migrations).
# App still exposes GET /health for manual checks.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 8
