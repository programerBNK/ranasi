# Used only when building the Chrome extension locally (`npm run zip`).
# Do NOT add these to Railway — the API service does not need WXT_* vars.
WXT_API_BASE=https://api.ranasi.com
WXT_CHECKOUT_URL=https://www.ranasi.com/pro
